# Used to run the automated tests in the tests/ folder.
pytest

# Optional terminal prompt helper for future CLI improvements.
questionary

# Runs the web dashboard UI and is the main Railway app framework.
streamlit

# Optional dependency for a future Telegram bot interface.
python-telegram-bot

# Optional dependency for future habit reminder scheduling.
APScheduler
