{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "streamlit run app.py --server.address 0.0.0.0 --server.port ${PORT:-8501}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
